import { NumberMinDirective } from './number-min.directive';

describe('NumberMinDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberMinDirective();
    expect(directive).toBeTruthy();
  });
});
